// This file has been autogenerated from a class added in the UI designer.

using System;
using Foundation;
using InformaticaTrentinaPCL.iOS.Helper;
using UIKit;

namespace InformaticaTrentinaPCL.iOS.Menu.Storyboard
{
	public partial class UITableViewCellMaster : UITableViewCell
	{

		public UITableViewCellMaster(IntPtr handle) : base(handle)
		{
		}
		public UITableViewCellMaster()
		{
		}

        public void Update(string desc, string imgName, bool selected,bool tab)
		{

			
            this.ContentView.BackgroundColor = (Utility.IsTablet() && UIApplication.SharedApplication.StatusBarOrientation.IsLandscape()) ? Colors.MASTER_LANDSCAPE : Colors.MASTER_PORTRAIT;
			
			label_title.Text = desc;
			var img = UIImage.FromBundle(imgName);
			
            if (img != null)
            {
                image.Image = img;
				heightImg.Constant = img.Size.Height;
                widthImg.Constant = img.Size.Width;
                image.TintColor = selected ? Colors.COLOR_NAVIGATION : UIColor.Black;
			}
            label_title.Text = tab ? label_title.Text.ToUpper() : label_title.Text;

              if (tab)
                Font.SetCustomStyleFont(label_title,Font.TITLE_BLACK,UITextAlignment.Left);
             else 
                Font.SetCustomStyleFont(label_title, Font.ROW_TABLE_SUB_TITLE_BLACK, UITextAlignment.Left);

			label_title.TextColor = selected?Colors.COLOR_NAVIGATION:UIColor.Black;
            viewSeleted.Hidden = !selected;
            leftconstraint.Constant = Utility.IsTablet() ? 10 : -20;
		}
	}
}
