// This file has been autogenerated from a class added in the UI designer.

using System;

using Foundation;
using InformaticaTrentinaPCL.Home.MVPD;
using InformaticaTrentinaPCL.iOS.Helper;
using InformaticaTrentinaPCL.OpenFile;
using InformaticaTrentinaPCL.OpenFile.MVP;
using UIKit;

namespace InformaticaTrentinaPCL.iOS.TabBar.Document.Open.Storyboard
{
	public partial class UITVCellOpenListDocument : UITableViewCell
	{
        private AbstractDocumentListItem docInfo;
        private IOpenDocumentPresenter presenter;

        public UITVCellOpenListDocument (IntPtr handle) : base (handle)
		{
		}

		public UITVCellOpenListDocument()
		{
		}

        public void setup(IOpenDocumentPresenter presenter)
        {
            this.presenter = presenter;
        }

        private void UpdateCell(AbstractDocumentListItem docInfo, bool isSelected)
        {
            this.docInfo = docInfo;
            labelDesc.Text = presenter.GetDocumentTitle(docInfo);

            imgArrowRight.Hidden = false;
            viewSeparator.Hidden = false;
            viewSeparator.BackgroundColor = Colors.FOOTER_SEPARATOR;
            Font.SetCustomStyleFont(labelDesc, Font.DETAILS, UITextAlignment.Left);

            if (isSelected)
            {
                ContentView.BackgroundColor = Colors.SELECTED_DOCUMENT_OPEN_SECTION_BACKGROUND;
                labelDesc.TextColor = UIColor.White;
                imgArrowRight.Image = imgArrowRight.Image.ImageWithRenderingMode(UIImageRenderingMode.AlwaysTemplate);
                imgArrowRight.TintColor = UIColor.White;
            }
        }

        public void UpdateAttachment(AbstractDocumentListItem docInfo, bool isSelected)
        {
            UpdateCell(docInfo, isSelected);
        }

        public void UpdatePrimaryDoc(AbstractDocumentListItem docInfo, bool isSelected)
        {
            UpdateCell(docInfo, isSelected);
            viewSeparator.Hidden = true;
        }
	}
}
