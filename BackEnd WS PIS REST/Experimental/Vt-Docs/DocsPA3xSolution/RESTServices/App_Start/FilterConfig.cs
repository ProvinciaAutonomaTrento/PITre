using System.Web.Mvc;

namespace RESTServices
{
    public class FilterConfig
    {
        public static void RegisterGlobalFilters(GlobalFilterCollection filters)
        {
            //filters.Add(new DocsPaActionFilterAttribute());
        }
    }
}